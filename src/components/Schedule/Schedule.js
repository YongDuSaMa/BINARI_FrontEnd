import React from 'react';
import styled from 'styled-components';

const Schedule = () => {
  return <ScheduleStyle>투병일지</ScheduleStyle>;
};

export default Schedule;

const ScheduleStyle = styled.div``;
